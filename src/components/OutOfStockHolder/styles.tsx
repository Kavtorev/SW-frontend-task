import styled from 'styled-components';
export const OutOfStockHolder = styled.p`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: var(--fs-m);
  text-transform: uppercase;
  color: var(--c-disabled);
`;
