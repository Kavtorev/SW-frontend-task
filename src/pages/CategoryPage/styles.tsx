import styled from 'styled-components';
export const CategoryPageTitle = styled.h1`
  margin-top: var(--m-s);
  margin-bottom: 0px;
  font-size: calc(var(--fs-l) + 0.45rem);
  font-weight: var(--fw-normal);
  line-height: 3.8;
`;

export const ProductsListWrapper = styled.div`
  margin-top: 5.4em;
`;
